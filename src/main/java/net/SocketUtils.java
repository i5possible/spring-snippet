package net;

/**
 * @author lianghong
 * @date 04/10/2018
 */

public class SocketUtils {
    /**
     * TCP (Transport Control Protocol
     * URL, URLConnection, Socket, ServerSocket
     * Server : ServerSocket(wait) newSocket(accept) Read/Write Data(receive send) Close Socket(close)
     * connect    I/O stream
     * Client : newSocket(request) Read/Write Data(send receive) Close Socket(close)
     */
    public static void main(String[] args) {

    }
}
